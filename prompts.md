# Журнал промптов (ИИ-агент: DeepSeek Chat)

## Этап 1. ТЗ и стек
ПРОМПТ:
«Я делаю учебный frontend-проект HabitFlow — трекер привычек. Помоги определить
технологический стек. Требования: современный фреймворк, простота для новичка,
работа через VS Code, данные в localStorage, нужны автотесты. Дай список библиотек
и обоснуй выбор.»
ОТВЕТ (кратко): React + TypeScript + Vite, React Router для роутинга,
Vitest + React Testing Library для тестов.

## Этап 2. Инициализация
ПРОМПТ:
«Объясни, что означает каждый файл в Vite-проекте (src, main.tsx, App.tsx,
vite.config.ts, index.html). И как настроить Vitest для React+TypeScript (jsdom).»
ОТВЕТ (кратко): описание структуры + настройка vite.config.ts с test.environment='jsdom'.

## Этап 3. Структура и роутинг
ПРОМПТ:
«Сгенерируй App.tsx с React Router: маршруты "/" (главная) и "/stats" (статистика).
И types.ts с интерфейсом Habit (id, name, color, days: number[], history, createdAt).»
ОТВЕТ (кратко): готовый код роутинга и типов.

## Этап 4. Компоненты
ПРОМПТ:
«Сгенерируй HabitCard.tsx (React+TS): пропс habit и колбэки onToggle/onDelete.
Отрисовывает название, цвет, счётчик streak 🔥, чекбокс отметки, кнопку удаления.
Чекбокс неактивен, если привычка не запланирована на сегодня. Отдельный CSS.»
ПРОМПТ:
«Сгенерируй HabitForm.tsx с валидацией: пустое название → ошибка "Введите название
привычки"; не выбран ни один день → ошибка "Выберите хотя бы один день недели".
Выбор цвета из палитры и дней недели.»
ОТВЕТ (кратко): код обоих компонентов со стилями.

## Этап 5. Тесты и отладка
ПРОМПТ:
«Напиши unit-тесты (Vitest) для функции calculateStreak: позитивный сценарий
(3 дня подряд → 3), негативный (пропуск дня → сброс), пустая история → 0.
И тесты для HabitCard: клик по чекбоксу вызывает onToggle(id).»
ПРОБЛЕМА:
Неоднозначность в подсчёте серии, если сегодня ещё не отмечено.
ПРОМПТ:
«Объясни, как корректно считать streak, если сегодняшний день ещё не отмечен,
но вчера был отмечен. Исправь логику.»
ОТВЕТ (кратко): начинать подсчёт от последнего отмеченного дня (сегодня или вчера).

## Этап 6. Адаптивность
ПРОМПТ:
«Сгенерируй CSS media queries для сетки привычек: 3 колонки (десктоп),
2 (≤768px), 1 (≤480px). Кнопки и зоны нажатия не меньше 44px.»
ОТВЕТ (кратко): готовые media queries.

## Этап 7. Оптимизация
ПРОМПТ:
«Проведи рефакторинг кода HabitFlow: найди дублирование, вынеси логику в чистые
функции, раздели крупные компоненты. Объясни изменения.»
ОТВЕТ (кратко): выделены storage.ts, ProgressBar, HabitList.

## Этап 8. Оформление
ПРОМПТ:
«Сгенерируй README.md для проекта HabitFlow: описание, технологии, функции,
инструкции запуска и тестов, структура проекта.»
ОТВЕТ (кратко): README.md (см. файл README.md).
